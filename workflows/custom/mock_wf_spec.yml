
type: workflow
info:
  id: mock-flow
  name: Mock Flow
  description: Mock flow
  author: Test

vars:
  my_var: 
    default: "InitialValue"
  res_file: 
    default: "result.txt"

workflow:
  - name: step_A
    module: custom/mock_tool_spec
    inputs:
      text: "FromFlow: {{my_var}}"
      outfile: "{{res_file}}"
