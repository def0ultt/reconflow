type: workflow
metadata:
  id: test-workflow
  name: Test Integration Workflow
  description: Verifies YAML module execution

variables:
  greet: "Hello From Workflow"
  fname: "workflow_out.txt"

modules:
  - id: step1
    module: test/echo
    inputs:
      message: "{{greet}}"
      filename: "{{fname}}"
