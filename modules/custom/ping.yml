type: module

info:
  id: ping
  name: test ping 
  tag: network
  description: Complete recon pipeline with parallel execution
  author: charlie

vars:
  target:
    required: true
  nub:
    default: 3

steps:
  # Phase 1: Parallel discovery
  - name: ping
    tool: ping
    args: "-c {{nub}} {{target}} "
  
  
  
