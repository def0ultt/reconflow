
type: module
metadata:
  id: mock-tool
  name: Mock Tool
  
inputs:
  text: "default_text"
  outfile: "out.txt"

steps:
  - id: write
    name: Write to File
    tool: echo
    args: '"{{inputs.text}}" >> {{project_path}}/{{inputs.outfile}}'
