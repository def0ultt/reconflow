type: module
metadata:
  id: test-echo
  name: Test Echo Module
  description: A simple module to test YAML integration
  author: ReconFlow

inputs:
  message: "Hello World"
  filename: "output.txt"

steps:
  - id: step1
    name: Echo to File
    tool: echo
    args: '"{{inputs.message}}" > {{project_path}}/{{inputs.filename}}'
    output:
      path: "{{project_path}}/{{inputs.filename}}"
