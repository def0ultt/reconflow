metadata:
  id: demo-flow
  name: Demo Import Flow
  description: A workflow that uses the imported demo-ping tool

variables:
  target: 1.1.1.1

modules:
  - id: check-alive
    module: custom/demo_tool
    inputs:
      target: "{{target}}"
      count: 1
