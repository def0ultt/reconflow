metadata:
  id: demo-ping
  name: Demo Ping Tool
  description: A specific ping tool imported from examples
  author: User

inputs:
  target: google.com
  count: 3

steps:
  - id: ping
    tool: ping
    args: "-c {{inputs.count}} {{inputs.target}}"
